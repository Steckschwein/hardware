*= $1000
jmp main 
!src <defs.h.a>
!src <bios.h.a>

spi 	  = $260
spidata   = spi
spistatus = spi+1
spictrl   = spi+1
spidiv    = spi+2
spiss     = spi+3

main 
	lda #$01
	sta spiss

	lda #$00
	sta spidiv

	lda #$00
	sta spictrl
	; sta spidata
	sta spistatus

	
	bit spistatus
	bpl +
	lda spidata
+


 	lda #$f0
 	sta spidata

--	bit spistatus
	bpl --
	; lda spidata
	
- 	jmp -
; hello
; 	!text "**Hello World!**", $00
