*=$1000

!src "../bios/defs.h.a"
!src "../bios/bios.h.a"

	sei

	stz chn_in

	lda #dev_lcd
  	sta chn_out

  	
	jsr lcdclear

-	jsr chrin
	cmp #$0d
	beq +
	jsr hexout
	jmp -
+	jsr hexout
	lda #$0a
	jsr hexout
	bra -
