*=$1000
!src "../bios/defs.h.a"
!src "../bios/bios.h.a"

	jsr lcdclear
	jsr spi_rw_byte
	jsr lcdhex
	jsr spi_rw_byte
	jsr lcdhex
	jsr spi_rw_byte
	jsr lcdhex
	jsr spi_rw_byte
	jsr lcdhex
	jsr spi_rw_byte
	jsr lcdhex
	jsr spi_rw_byte
	jsr lcdhex
-	bra - 

	lda #$00
	tax
	inx

-
	sta via1portb
	stx via1portb
	sta via1portb
	stx via1portb
	sta via1portb
	stx via1portb
	sta via1portb
	stx via1portb
	sta via1portb
	stx via1portb
	sta via1portb
	stx via1portb
	sta via1portb
	stx via1portb
	sta via1portb
	stx via1portb


	bra -

