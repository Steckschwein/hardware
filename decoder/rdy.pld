GAL16V8    
RDYGEN

; PIN assignment
;
Clock LORAM  HIRAM  VIA  UART VDP SND NC   Clear GND
/OE   W1     W2     NC   NC   RDY NC  Q1   Q0    VCC
;
;  boolean expressions

; TODO: simplify decoding

W2		= LORAM * HIRAM * VIA * UART * SND * /VDP 
		+ LORAM * HIRAM * VIA * UART * /SND * VDP 
W1 		= W2 
		+ LORAM * HIRAM * VIA * UART * VDP 

; Q0.R 	= Clear * /Q0  
; Q1.R 	= Clear * /Q1 * Q0
       	; + Clear *  Q1 * /Q0
; 
; RDY = W1 * /W2 * Q0 
	; + /W1 * W2 * Q1 
	; + /W1 * /W2


Q0.R 	= W1 * /Q0 * /Q1 * Clear
	+ W2 * /Q0 * /Q1 * Clear

Q1.R 	= W2 * /Q1 * Q0 * Clear   		


RDY 	= /Q0 * /Q1


DESCRIPTION:
Generation of /RDY line using modeled after http://sbc.rictor.org/parts/wsgen.html
and adapted to galasm syntax
